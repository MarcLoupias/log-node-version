{
  "name": "log-node-version",
  "version": "1.0.0",
  "description": "Simply log the Node.js running version in the console. Log also a separator line after.",
  "author": "marlou <pro@marc-loupias.fr>",
  "private": true,
  "license": "ISC",
  "engines": {
    "node": ">=6.0.0",
    "npm": ">=5.2.0"
  },
  "scripts": {
    "test": "node test",
    "test-against-node-6": "npx -p node@6.14.2 -- node test",
    "test-against-node-8": "npx -p node@8.0.0 -- node test",
    "test-against-node-10": "npx -p node@10 -- node test"
  }
}
